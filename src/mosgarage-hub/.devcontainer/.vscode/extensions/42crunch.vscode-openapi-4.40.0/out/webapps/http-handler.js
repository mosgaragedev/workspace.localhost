"use strict";
/*
 Copyright (c) 42Crunch Ltd. All rights reserved.
 Licensed under the GNU Affero General Public License version 3. See LICENSE.txt in the project root for license information.
*/
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.executeHttpRequest = executeHttpRequest;
exports.executeHttpRequestRaw = executeHttpRequestRaw;
const http_1 = __importDefault(require("http"));
const https_1 = __importDefault(require("https"));
const got_1 = __importDefault(require("got"));
const form_data_1 = __importDefault(require("form-data"));
const proxy_1 = require("../proxy");
async function executeHttpRequest(id, request, config) {
    try {
        const response = await executeHttpRequestRaw(request, config);
        return {
            command: "showHttpResponse",
            payload: { id, response },
        };
    }
    catch (e) {
        return {
            command: "showHttpError",
            payload: { id, error: e },
        };
    }
}
async function executeHttpRequestRaw(request, config) {
    const { url, method, headers, body } = request;
    const restoredBody = restoreBody(body, getContentType(headers));
    // drop Content-Type header if multipart/form-data
    // correct Content-Type will be generated by the client, and will include
    // proper boundary
    if (isMultipartFormData(body, getContentType(headers))) {
        for (const name of Object.keys(headers)) {
            if (name.toLowerCase() == "content-type") {
                delete headers[name];
            }
        }
    }
    const isHttpsUrl = url.toLowerCase().startsWith("https://");
    const proxy = await (0, proxy_1.createProxyAgentsAndCerts)(config.https?.proxy);
    const requestFn = proxy !== undefined
        ? isHttpsUrl
            ? https_1.default.__vscodeOriginal?.request
            : http_1.default.__vscodeOriginal?.request
        : undefined;
    const options = {
        throwHttpErrors: false,
        method,
        body: restoredBody,
        headers: {
            ...headers,
        },
        https: {
            rejectUnauthorized: config?.https?.rejectUnauthorized ?? true,
            certificateAuthority: proxy !== undefined ? proxy.certs : undefined,
        },
        retry: {
            limit: 0,
        },
        request: requestFn,
        agent: proxy?.agents,
    };
    try {
        const response = await (0, got_1.default)(url, options);
        const responseHeaders = [];
        for (let i = 0; i < response.rawHeaders.length; i += 2) {
            responseHeaders.push([response.rawHeaders[i], response.rawHeaders[i + 1]]);
        }
        return {
            statusCode: response.statusCode,
            statusMessage: response.statusMessage,
            body: response.body,
            httpVersion: response.httpVersion,
            headers: responseHeaders,
        };
    }
    catch (e) {
        const { code, message } = e;
        const sslError = isSslError(code);
        throw {
            code,
            message,
            sslError,
        };
    }
}
function getContentType(headers) {
    for (const [key, value] of Object.entries(headers)) {
        const name = key.toLowerCase();
        if (name == "content-type") {
            return value;
        }
    }
}
function isMultipartFormData(body, contentType) {
    return body && contentType === "multipart/form-data";
}
function restoreBody(body, contentType) {
    if (isMultipartFormData(body, contentType)) {
        const form = new form_data_1.default();
        for (const [key, value] of body) {
            form.append(key, value);
        }
        return form;
    }
    return body;
}
function isSslError(code) {
    const codes = [
        "UNABLE_TO_GET_ISSUER_CERT",
        "UNABLE_TO_GET_CRL",
        "UNABLE_TO_DECRYPT_CERT_SIGNATURE",
        "UNABLE_TO_DECRYPT_CRL_SIGNATURE",
        "UNABLE_TO_DECODE_ISSUER_PUBLIC_KEY",
        "CERT_SIGNATURE_FAILURE",
        "CRL_SIGNATURE_FAILURE",
        "CERT_NOT_YET_VALID",
        "CERT_HAS_EXPIRED",
        "CRL_NOT_YET_VALID",
        "CRL_HAS_EXPIRED",
        "ERROR_IN_CERT_NOT_BEFORE_FIELD",
        "ERROR_IN_CERT_NOT_AFTER_FIELD",
        "ERROR_IN_CRL_LAST_UPDATE_FIELD",
        "ERROR_IN_CRL_NEXT_UPDATE_FIELD",
        "OUT_OF_MEM",
        "DEPTH_ZERO_SELF_SIGNED_CERT",
        "SELF_SIGNED_CERT_IN_CHAIN",
        "UNABLE_TO_GET_ISSUER_CERT_LOCALLY",
        "UNABLE_TO_VERIFY_LEAF_SIGNATURE",
        "CERT_CHAIN_TOO_LONG",
        "CERT_REVOKED",
        "INVALID_CA",
        "PATH_LENGTH_EXCEEDED",
        "INVALID_PURPOSE",
        "CERT_UNTRUSTED",
        "CERT_REJECTED",
        "HOSTNAME_MISMATCH",
    ];
    return codes.includes(code);
}
//# sourceMappingURL=http-handler.js.map