"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.supportsNativeRuntime = supportsNativeRuntime;
exports.ensureBinary = ensureBinary;
exports.ensureNativeLibrary = ensureNativeLibrary;
exports.writeConfigFile = writeConfigFile;
const promises_1 = require("fs/promises");
const path_1 = __importDefault(require("path"));
const errors_1 = require("./errors");
const TLS_CLIENT_API_RELEASES = "https://api.github.com/repos/bogdanfinn/tls-client-api/releases";
const TLS_CLIENT_RELEASES = "https://api.github.com/repos/bogdanfinn/tls-client/releases";
function supportsNativeRuntime(platform = process.platform, arch = process.arch) {
    return ((platform === "win32" && (arch === "x64" || arch === "ia32"))
        || (platform === "darwin" && (arch === "arm64" || arch === "x64"))
        || (platform === "linux" && (arch === "x64" || arch === "arm64")));
}
function getApiBinaryAssetSpec() {
    const platform = process.platform;
    const arch = process.arch;
    if (platform === "darwin" && arch === "arm64") {
        return {
            releaseBaseUrl: TLS_CLIENT_API_RELEASES,
            prefix: "tls-client-api-darwin-arm64-",
            suffix: "",
        };
    }
    if (platform === "darwin" && arch === "x64") {
        return {
            releaseBaseUrl: TLS_CLIENT_API_RELEASES,
            prefix: "tls-client-api-darwin-amd64-",
            suffix: "",
        };
    }
    if (platform === "linux" && arch === "x64") {
        return {
            releaseBaseUrl: TLS_CLIENT_API_RELEASES,
            prefix: "tls-client-api-linux-amd64-",
            suffix: "",
        };
    }
    if (platform === "win32" && arch === "x64") {
        return {
            releaseBaseUrl: TLS_CLIENT_API_RELEASES,
            prefix: "tls-client-api-windows-64-",
            suffix: ".exe",
        };
    }
    if (platform === "win32" && arch === "ia32") {
        return {
            releaseBaseUrl: TLS_CLIENT_API_RELEASES,
            prefix: "tls-client-api-windows-32-",
            suffix: ".exe",
        };
    }
    throw new errors_1.TLSClientError(`Unsupported platform for tls-client-api: ${platform}/${arch}`, { code: "ERR_UNSUPPORTED_PLATFORM" });
}
function getNativeLibraryAssetSpec() {
    const platform = process.platform;
    const arch = process.arch;
    if (platform === "darwin" && arch === "arm64") {
        return {
            releaseBaseUrl: TLS_CLIENT_RELEASES,
            prefix: "tls-client-darwin-arm64-",
            suffix: ".dylib",
        };
    }
    if (platform === "darwin" && arch === "x64") {
        return {
            releaseBaseUrl: TLS_CLIENT_RELEASES,
            prefix: "tls-client-darwin-amd64-",
            suffix: ".dylib",
        };
    }
    if (platform === "linux" && arch === "x64") {
        return {
            releaseBaseUrl: TLS_CLIENT_RELEASES,
            prefix: "tls-client-linux-ubuntu-amd64-",
            suffix: ".so",
        };
    }
    if (platform === "linux" && arch === "arm64") {
        return {
            releaseBaseUrl: TLS_CLIENT_RELEASES,
            prefix: "tls-client-linux-arm64-",
            suffix: ".so",
        };
    }
    if (platform === "win32" && arch === "x64") {
        return {
            releaseBaseUrl: TLS_CLIENT_RELEASES,
            prefix: "tls-client-windows-64-",
            suffix: ".dll",
        };
    }
    if (platform === "win32" && arch === "ia32") {
        return {
            releaseBaseUrl: TLS_CLIENT_RELEASES,
            prefix: "tls-client-windows-32-",
            suffix: ".dll",
        };
    }
    throw new errors_1.TLSClientError(`Unsupported platform for tls-client native library: ${platform}/${arch}`, { code: "ERR_UNSUPPORTED_PLATFORM" });
}
function getDefaultDownloadDir(options) {
    return options.downloadDir ?? path_1.default.resolve(__dirname, "..", "bin");
}
function normalizeVersion(version) {
    return version.replace(/^v/i, "");
}
function createHeaders(token) {
    const headers = {
        Accept: "application/vnd.github+json",
        "User-Agent": "tls-client-node",
    };
    if (token) {
        headers.Authorization = `Bearer ${token}`;
    }
    return headers;
}
async function fetchReleaseMetadata(releaseBaseUrl, version, token) {
    const url = version && version !== "latest"
        ? `${releaseBaseUrl}/tags/v${normalizeVersion(version)}`
        : `${releaseBaseUrl}/latest`;
    const response = await globalThis.fetch(url, {
        headers: createHeaders(token),
    });
    if (!response.ok) {
        throw new errors_1.TLSClientError(`Failed to resolve release metadata: ${response.status} ${response.statusText}`, { code: "ERR_RELEASE_LOOKUP", status: response.status });
    }
    return response.json();
}
async function fileExists(filePath) {
    try {
        await (0, promises_1.access)(filePath);
        return true;
    }
    catch {
        return false;
    }
}
async function findExistingAsset(directory, assetSpec, requestedVersion) {
    if (!(await fileExists(directory))) {
        return null;
    }
    const entries = await (0, promises_1.readdir)(directory);
    const matches = entries
        .filter((entry) => entry.startsWith(assetSpec.prefix) && entry.endsWith(assetSpec.suffix))
        .sort();
    if (matches.length === 0) {
        return null;
    }
    const normalizedVersion = requestedVersion
        ? normalizeVersion(requestedVersion)
        : undefined;
    const selected = normalizedVersion
        ? matches.find((entry) => entry === `${assetSpec.prefix}${normalizedVersion}${assetSpec.suffix}`)
        : matches[matches.length - 1];
    if (!selected) {
        return null;
    }
    return {
        filePath: path_1.default.join(directory, selected),
        version: selected.slice(assetSpec.prefix.length, selected.length - assetSpec.suffix.length),
    };
}
async function downloadBinaryAsset(assetUrl, destinationPath) {
    const response = await globalThis.fetch(assetUrl, {
        headers: {
            "User-Agent": "tls-client-node",
        },
    });
    if (!response.ok) {
        throw new errors_1.TLSClientError(`Failed to download release asset: ${response.status} ${response.statusText}`, { code: "ERR_BINARY_DOWNLOAD", status: response.status });
    }
    const arrayBuffer = await response.arrayBuffer();
    await (0, promises_1.writeFile)(destinationPath, Buffer.from(arrayBuffer));
    if (process.platform !== "win32") {
        await (0, promises_1.chmod)(destinationPath, 0o755);
    }
}
async function ensureBinary(options = {}) {
    if (options.binaryPath) {
        return {
            executablePath: options.binaryPath,
            version: normalizeVersion(options.version ?? "custom"),
        };
    }
    const directory = getDefaultDownloadDir(options);
    await (0, promises_1.mkdir)(directory, { recursive: true });
    const assetSpec = getApiBinaryAssetSpec();
    const existing = await findExistingAsset(directory, assetSpec, options.version);
    if (existing) {
        return {
            executablePath: existing.filePath,
            version: existing.version,
        };
    }
    const metadata = await fetchReleaseMetadata(assetSpec.releaseBaseUrl, options.version, options.githubToken);
    const version = normalizeVersion(metadata.tag_name ?? options.version ?? "latest");
    const assetName = `${assetSpec.prefix}${version}${assetSpec.suffix}`;
    const asset = metadata.assets?.find((entry) => entry.name === assetName);
    if (!asset?.browser_download_url) {
        throw new errors_1.TLSClientError(`No tls-client-api release asset found for ${assetName}`, { code: "ERR_BINARY_ASSET_MISSING" });
    }
    const destinationPath = path_1.default.join(directory, assetName);
    await downloadBinaryAsset(asset.browser_download_url, destinationPath);
    return {
        executablePath: destinationPath,
        version,
    };
}
async function ensureNativeLibrary(options = {}) {
    if (options.nativeLibraryPath) {
        return {
            libraryPath: options.nativeLibraryPath,
            version: normalizeVersion(options.version ?? "custom"),
        };
    }
    const directory = getDefaultDownloadDir(options);
    await (0, promises_1.mkdir)(directory, { recursive: true });
    const assetSpec = getNativeLibraryAssetSpec();
    const existing = await findExistingAsset(directory, assetSpec, options.version);
    if (existing) {
        return {
            libraryPath: existing.filePath,
            version: existing.version,
        };
    }
    const metadata = await fetchReleaseMetadata(assetSpec.releaseBaseUrl, options.version, options.githubToken);
    const version = normalizeVersion(metadata.tag_name ?? options.version ?? "latest");
    const assetName = `${assetSpec.prefix}${version}${assetSpec.suffix}`;
    const asset = metadata.assets?.find((entry) => entry.name === assetName);
    if (!asset?.browser_download_url) {
        throw new errors_1.TLSClientError(`No tls-client release asset found for ${assetName}`, { code: "ERR_BINARY_ASSET_MISSING" });
    }
    const destinationPath = path_1.default.join(directory, assetName);
    await downloadBinaryAsset(asset.browser_download_url, destinationPath);
    return {
        libraryPath: destinationPath,
        version,
    };
}
async function writeConfigFile(directory, port, healthPort, apiKey) {
    await (0, promises_1.mkdir)(directory, { recursive: true });
    const configPath = path_1.default.join(directory, "config.dist.yml");
    const yaml = [
        "env: dev",
        "",
        "app_project: tls-client",
        "app_family: tls-client",
        "app_name: api",
        "",
        "log:",
        "  handlers:",
        "    main:",
        "      formatter: console",
        "      level: info",
        "      type: iowriter",
        "      writer: stdout",
        "  timestamp_format: '15:04:05:000'",
        "",
        "sentry:",
        "  dsn: ''",
        "  release: ''",
        "  tags:",
        "    project: tls-client",
        "    component: tls-client-api",
        "",
        "api:",
        `  port: ${port}`,
        "  mode: release",
        "  health:",
        `    port: ${healthPort}`,
        "  timeout:",
        "    read: 120s",
        "    write: 120s",
        "    idle: 120s",
        "",
        `api_auth_keys: [\"${apiKey}\"]`,
        'api_cors_allowed_origin_pattern: ""',
        'api_cors_allowed_headers: ["X-API-KEY", "X-API-VIEW", "Content-Type"]',
        'api_cors_allowed_methods: ["POST", "GET", "PUT", "DELETE"]',
        "",
    ].join("\n");
    await (0, promises_1.writeFile)(configPath, yaml, { encoding: "utf8", mode: 0o600 });
    if (process.platform !== "win32") {
        await (0, promises_1.chmod)(configPath, 0o600);
    }
    return configPath;
}
