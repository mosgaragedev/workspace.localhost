import { StreamAnimationGranularity, StreamSmoothingPreset } from "./types.mjs";
import { Options } from "react-markdown";
//#region src/Streamdown.d.ts
interface StreamdownProps extends Omit<Options, 'children'> {
  content: string;
  granularity?: StreamAnimationGranularity;
  /**
   * Hold the previous frame while the trailing LaTeX formula is still
   * incomplete, preventing raw-source flashes mid-stream. Enable when the
   * pipeline renders math (remark-math + rehype-katex).
   */
  latexGuard?: boolean;
  preprocess?: (text: string) => string;
  smoothing?: StreamSmoothingPreset;
}
declare const Streamdown: import("react").NamedExoticComponent<StreamdownProps>;
//#endregion
export { Streamdown, Streamdown as default, StreamdownProps };