import { jsx } from "react/jsx-runtime";
//#region src/styles.tsx
const STREAM_FADE_DURATION = 180;
const STREAMDOWN_ANIMATED_CLASS = "streamdown-animated";
const css = `
@keyframes streamdown-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}
.${STREAMDOWN_ANIMATED_CLASS} .stream-char,
.${STREAMDOWN_ANIMATED_CLASS} .stream-block {
  opacity: 0;
  animation-name: streamdown-fade-in;
  animation-duration: 180ms;
  animation-timing-function: cubic-bezier(0.33, 0, 0.67, 1);
  animation-fill-mode: forwards;
}
.${STREAMDOWN_ANIMATED_CLASS} .stream-char-revealed {
  opacity: 1;
  animation: none;
}
.${STREAMDOWN_ANIMATED_CLASS} .katex-display .katex-html span {
  mask: none !important;
  animation: none !important;
}
@media (prefers-reduced-motion: reduce) {
  .${STREAMDOWN_ANIMATED_CLASS} .stream-block {
    opacity: 1;
    animation: none;
  }
}
`;
const StreamdownStyles = () => /* @__PURE__ */ jsx("style", {
	href: "lobehub-streamdown",
	precedence: "default",
	children: css
});
//#endregion
export { STREAMDOWN_ANIMATED_CLASS, STREAM_FADE_DURATION, StreamdownStyles };
